# 📋 ระบบยอดค้างชำระ (Outstanding Balance System)

## 🎯 วัตถุประสงค์
ระบบนี้ช่วยให้ผู้ใช้สามารถ:
- **แบ่งจ่าย** ค่าใช้จ่ายต่าง ๆ ในใบแจ้งหนี้
- **สะสมยอดค้าง** ไปยังใบแจ้งหนี้เดือนถัดไป
- **คิดค่าปรับ** อัตโนมัติสำหรับยอดค้างที่เลยกำหนด
- **ติดตาม** ประวัติการชำระเงินอย่างละเอียด

## 🏗️ โครงสร้างระบบ

### 📊 Database Schema (เพิ่มเติม)
```sql
-- เพิ่มฟิลด์ใน invoice table
ALTER TABLE invoice 
ADD COLUMN previous_balance INTEGER DEFAULT 0,    -- ยอดค้างจากเดือนก่อน
ADD COLUMN paid_amount INTEGER DEFAULT 0,         -- ยอดที่ชำระแล้ว
ADD COLUMN remaining_balance INTEGER DEFAULT 0;   -- ยอดคงเหลือ
```

### 🔧 ไฟล์ใหม่ที่สร้าง

#### Backend
1. **OutstandingBalanceService.java** - หัวใจของระบบ
2. **OutstandingBalanceController.java** - API endpoints
3. **PdfStyleService.java** - จัดการ PDF styling 
4. **outstanding_balance_migration.sql** - Database migration

#### Frontend (จะต้องสร้างเพิ่ม)
- Component สำหรับแสดงยอดค้าง
- หน้าจัดการการชำระเงิน
- ปุ่มสร้างใบแจ้งหนี้แบบรวมยอดค้าง

## 📋 วิธีการใช้งาน

### 1. สร้างใบแจ้งหนี้แบบรวมยอดค้าง

**API Request:**
```javascript
POST /invoice/create
{
  "contractId": 1,
  "includeOutstandingBalance": true,  // 🆕 ฟีเจอร์ใหม่
  "rentAmount": 8000,
  "water": 200,
  "electricity": 500
}
```

**ผลลัพธ์:**
- ระบบจะคำนวณยอดค้างจากใบแจ้งหนี้ก่อนหน้าอัตโนมัติ
- สร้างใบแจ้งหนี้ใหม่ที่รวมยอดค้าง + ค่าใช้จ่ายเดือนปัจจุบัน

### 2. บันทึกการชำระเงิน (แบ่งจ่าย)

**API Request:**
```javascript
POST /outstanding-balance/invoice/{invoiceId}/payment
{
  "paymentAmount": 4000,           // จ่าย 4,000 จากยอดรวม 8,700
  "paymentMethod": "BANK_TRANSFER",
  "notes": "โอนเงินผ่านธนาคาร"
}
```

**ผลลัพธ์:**
- อัพเดท `paid_amount` = 4000
- อัพเดท `remaining_balance` = 4700 (เหลือค้าง)
- สถานะใบแจ้งหนี้ยังคงเป็น "ยังไม่ชำระครบ"

### 3. คิดค่าปรับอัตโนมัติ

**เงื่อนไข:**
- ใบแจ้งหนี้เลยวันครบกำหนด 30 วัน
- ยังมียอดคงเหลือ > 0
- ยังไม่เคยคิดค่าปรับ

**การทำงาน:**
```java
// คิดค่าปรับ 10% ของยอดคงเหลือ
int penalty = (int) Math.round(remainingAmount * 0.10);
```

### 4. ตรวจสอบยอดค้าง

**API Request:**
```javascript
GET /outstanding-balance/contract/{contractId}/summary
```

**Response:**
```json
{
  "totalOutstanding": 4700,
  "totalPenalty": 470,
  "overdueCount": 1,
  "totalInvoices": 3
}
```

## 🔄 Flow การทำงาน

### กรณีที่ 1: ชำระครบ
1. ลูกค้าจ่าย 8,700 บาท (ครบตามยอด)
2. `paid_amount` = 8700, `remaining_balance` = 0
3. สถานะเปลี่ยนเป็น "ชำระแล้ว"
4. ใบแจ้งหนี้เดือนถัดไป → ไม่มียอดค้าง

### กรณีที่ 2: ชำระบางส่วน
1. ลูกค้าจ่าย 4,000 บาท
2. `paid_amount` = 4000, `remaining_balance` = 4700
3. สถานะยังคงเป็น "ยังไม่ชำระครบ"
4. ใบแจ้งหนี้เดือนถัดไป → `previous_balance` = 4700

### กรณีที่ 3: ไม่ชำระ + เลยกำหนด
1. ลูกค้าไม่จ่ายเงิน + เลย 30 วัน
2. ระบบคิดค่าปรับ 10% = 870 บาท
3. `penalty_total` = 870, `net_amount` = 9570
4. ใบแจ้งหนี้เดือนถัดไป → `previous_balance` = 9570

## 📄 PDF ใบแจ้งหนี้ใหม่

### ฟีเจอร์ที่เพิ่ม:
- **แสดงยอดค้างจากเดือนก่อน** แยกบรรทัด
- **สรุปยอดแบบละเอียด**:
  - ยอดค้างจากเดือนก่อน: 4,700 บาท
  - ยอดรวมค่าบริการเดือนนี้: 8,700 บาท
  - ค่าปรับล่าช้า: 470 บาท
  - **ยอดรวมสุทธิ: 13,870 บาท**

### สีสถานะ:
- 🔴 **แดงอ่อน**: ยังไม่ชำระเงิน
- 🟢 **เขียวอ่อน**: ชำระเงินเรียบร้อยแล้ว
- ⚫ **เทาอ่อน**: ยกเลิกแล้ว

## 🔌 API Endpoints

### Outstanding Balance
```
GET    /outstanding-balance/contract/{contractId}/summary
GET    /outstanding-balance/contract/{contractId}/invoices
GET    /outstanding-balance/contract/{contractId}/calculate
POST   /outstanding-balance/invoice/{invoiceId}/payment
```

### Invoice (ปรับปรุง)
```
POST   /invoice/create
- เพิ่มพารามิเตอร์ "includeOutstandingBalance": true
```

## ⚠️ ข้อควรระวัง

### 1. **ความปลอดภัย**
- ตรวจสอบ Contract ownership ก่อนการดำเนินการ
- Validate จำนวนเงินที่ชำระ (ต้อง > 0)
- ป้องกัน Double payment

### 2. **ประสิทธิภาพ**
- ใช้ Database transaction สำหรับการอัพเดทหลายตาราง
- Cache ผลลัพธ์การคำนวณยอดค้าง
- Index ที่เหมาะสมสำหรับ query ที่ใช้บ่อย

### 3. **ความถูกต้องของข้อมูล**
- คำนวณยอดค้างใหม่หลังจากมีการชำระเงิน
- ตรวจสอบความสอดคล้องระหว่าง `net_amount` และ `paid_amount + remaining_balance`
- Audit log สำหรับการเปลี่ยนแปลงที่สำคัญ

## 🚀 การขยายระบบในอนาคต

1. **Payment Plan**: แผนการชำระเงินแบบผ่อน
2. **Auto Deduction**: หักเงินอัตโนมัติ
3. **SMS/Email Reminder**: แจ้งเตือนก่อนครบกำหนด
4. **Discount System**: ระบบส่วนลดสำหรับการชำระทันเวลา
5. **Reporting**: รายงานการวิเคราะห์การชำระเงิน

---

## 🔍 ตัวอย่างการใช้งานจริง

**สถานการณ์:** ห้อง 101 ค่าเช่า 8,000 บาท
1. **เดือน 1**: สร้างใบแจ้งหนี้ 8,700 บาท → ลูกค้าจ่าย 4,000 บาท → ค้าง 4,700 บาท
2. **เดือน 2**: สร้างใบแจ้งหนี้รวมยอดค้าง = 4,700 + 8,700 = 13,400 บาท
3. **เดือน 2**: ลูกค้าจ่าย 13,400 บาท → ชำระครบทุกยอด
4. **เดือน 3**: สร้างใบแจ้งหนี้ปกติ 8,700 บาท (ไม่มียอดค้าง)

ระบบนี้จะช่วยให้การจัดการการเงินมีความยืดหยุ่นและติดตามได้อย่างแม่นยำครับ! 💪